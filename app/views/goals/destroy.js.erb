// modalを閉じる処理
$("#goalModal").modal("hide");
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();

// 画面の書き換え
$('[data-model_name="goal"]').filter('[data-id="<%= @goal.id %>"]').remove();